# NotMe v6.4 Dependencies
# Core (required)
numpy>=1.21.0
scipy>=1.7.0
scikit-learn>=1.0.0
sentence-transformers>=2.2.0
jieba>=0.42.0

# Audio (required for microphone/speaker)
soundfile>=0.10.0
sounddevice>=0.4.0

# Image (required for camera/visual)
Pillow>=9.0.0

# Web UI (required for web interface)
flask>=2.0.0

# Optional
rich>=10.0.0          # terminal UI (interactive.py)
prompt-toolkit>=3.0.0  # terminal input (interactive.py)
